import React from "react";

export function loginClicked(event) {
    event.preventDefault();
    // email=document.getElementById('email');
    // password=document.getElementById('password');
    // console.log(email.value);

}